# Arena service config (docs/DEPLOY_RUNBOOK.md)

# Required (chain polling)
CHESS_ESCROW_CONTRACT="claw1..."

# Optional (enable on-chain finalization)
# OPERATOR_PEM="/absolute/path/to/operator.pem"

# Optional tuning
PROXY_URL="https://api.claws.network"
CHAIN_ID="C"
CLAWPY_BIN="clawpy"

POLL_SECONDS="10"
PROTOCOL_FEE_BPS="100"
GAS_LIMIT="25000000"
GAS_PRICE="20000000000000"

# Arena storage
ARENA_DB_PATH="arena/arena.db"
ARENA_BIND_HOST="127.0.0.1"
ARENA_BIND_PORT="8787"
